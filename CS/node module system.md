#cs #web  #node 

-   export
    ```jsx
    module.exports = {
      handler: requestHandler,
      text: 'some text'
    };
    ```
-   import
    ```jsx
    const route = require(./routes);
    ```
    
    → 如果是.js 檔，則可以省略.js